# Custom Configuration Example for VITE_CONFIG_JSON

# You can override the following fields (fields that presets.js doesn't change):
# 
# Top-level fields:
# - domain: Custom domain name (defaults to window.location.hostname)
# - html_title: Custom page title (defaults to "<domain> | <error_code>: <title>")
# - time: Custom timestamp (defaults to current UTC time)
# - ray_id: Custom Cloudflare Ray ID (defaults to random hex)
# - client_ip: Custom client IP address (defaults to '127.0.0.1')
#
# Nested location fields (within status objects):
# - browser_status.location: Custom location for Browser component
# - cloudflare_status.location: Custom location for Cloudflare component
# - host_status.location: Custom location for Host component

# Example usage:
# VITE_CONFIG_JSON='{"domain":"example.com","ray_id":"abc123","client_ip":"1.2.3.4","browser_status":{"location":"Beijing"},"cloudflare_status":{"location":"Hong Kong"},"host_status":{"location":"Shanghai"}}'

# Multi-line format example (for readability, not actual usage):
# {
#   "domain": "example.com",
#   "ray_id": "abc123def456",
#   "client_ip": "203.0.113.5",
#   "browser_status": {
#     "location": "Beijing"
#   },
#   "cloudflare_status": {
#     "location": "Hong Kong"
#   },
#   "host_status": {
#     "location": "Shanghai"
#   }
# }

# Note: The following fields CANNOT be customized via VITE_CONFIG_JSON 
# (they are controlled by presets.js):
# - title, error_code
# - browser_status.status, browser_status.status_text, browser_status.name
# - cloudflare_status.status, cloudflare_status.status_text, cloudflare_status.name
# - host_status.status, host_status.status_text, host_status.name
# - error_source, what_happened, what_can_i_do, more_information, perf_sec_by
